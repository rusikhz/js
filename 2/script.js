"use strict"
let number = prompt('Введите число',0)
if(number % 2 === 0) {
    alert(true)
  } else {
    alert(false)
  }
/*use strict-строгий режим(код без ошибок)
let number-обьявление переменной, промпт окошко выводит, ноль значение начальное
снизу проверка условий, alert окошко со словом
*/ 